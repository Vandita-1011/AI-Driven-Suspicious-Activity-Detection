"""
Tests Package
=============
Unit and integration tests for the AI engine.

Run all tests:
    pytest tests/ -v

Run with coverage:
    pytest tests/ -v --cov=src --cov-report=term-missing

Note: Run from the project root directory so that the `src` package
is on the Python path.  On Windows:
    set PYTHONPATH=.
    pytest tests/ -v
"""
